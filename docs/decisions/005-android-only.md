# 005 — Android only

Flutter e React Native saíram do tree. `legacy/flutter` e `apps/rn` não estão mais no working tree. O histórico git guarda o código. Não reabrir.

Stack viva = `apps/android` + `server`.

Skills de produto = `dieta-bot-android-*` em `.grok/skills/`. Os nomes `nutri-*` e as skills Flutter saíram.

Oficiais Google ficam as instaladas via npx. Não recriar `android-architecture`, `material3-expressive` nem `kotlin-clean`.
