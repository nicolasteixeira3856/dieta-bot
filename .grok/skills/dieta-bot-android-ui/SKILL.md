---
name: dieta-bot-android-ui
description: Constrói UI Nutri em Compose. Use quando criar ou alterar O1 O2 T0 T1 T2 T3, sheet, composer, saldo, ButtonGroup.
---

# dieta-bot-android-ui

Copy do wire. Número primeiro. Sem coach.

Tokens: `docs/tokens.md`.
Theme: `MaterialExpressiveTheme` + `MotionScheme.expressive()`.

Saldo 34pt. CTA `#f3f5f7`/`#111`. Sheet radius 22 top. Card 14.

O1 O2 T3 modos = ButtonGroup. Loading = LoadingIndicator.

Chat é sheet. Home não é chat. Dia 1 zero chips.

Screenshot `adb exec-out screencap -p > docs/qa/android-<tela>.png`.
