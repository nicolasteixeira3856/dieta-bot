---
name: dieta-bot-android-ui
description: Build Nutri UI in Compose. Use when creating or changing O1 O2 T0 T1 T2 T3, sheet, composer, remaining, ButtonGroup.
---

# dieta-bot-android-ui

Copy from the wire. Number first. No coach.

Tokens: `docs/tokens.md`.
Theme: `MaterialExpressiveTheme` + `MotionScheme.expressive()`.

Remaining 34pt. CTA `#f3f5f7`/`#111`. Sheet radius 22 top. Card 14.

O1 O2 T3 modes = ButtonGroup. Loading = LoadingIndicator.

Chat is a sheet. Home is not chat. Day 1 zero chips.

Screenshot with `cmd /c "adb exec-out screencap -p > docs\qa\android\current\dark\<id>.png"`.
